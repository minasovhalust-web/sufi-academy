/**
 * Live entity re-exports — stable module-local import paths.
 * Prisma generates the full types from schema.prisma.
 */
export { LiveSession, LiveParticipant, SessionStatus, ParticipantRole } from '@prisma/client';
