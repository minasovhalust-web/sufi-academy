/**
 * Video entity — re-exported from @prisma/client for module-local access.
 *
 * Prisma generates the full Video type from schema.prisma.
 * This file provides a stable import path within the videos module so
 * consumers don't need to import directly from @prisma/client.
 */
export { Video, VideoStatus } from '@prisma/client';
